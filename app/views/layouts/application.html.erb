<!DOCTYPE html>
<html class="no-js" lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <section id="header">
      <h1><span class="heavy">ANDREW</span> BANGEL</h1>
      <h2>CHARACTER ANIMATOR</h2>
      <div class="navbar">
        <ul>
          <li<%= ' class=selected' if @pageclass == "demoreel" %>><%= link_to "REEL", root_path %></li>
          <li<%= ' class=selected' if @pageclass == "works"    %>><%= link_to "WORKS", works_path %></li>
          <li<%= ' class=selected' if @pageclass == "bio"      %>><%= link_to "ABOUT", bios_path %></li>
          <li<%= ' class=selected' if @pageclass == "contact"  %>><%= link_to "CONTACT", contacts_path %></li>
        </ul>
      </div>
    </section>

    <p id="notice"><%= notice %></p>

    <section id="<%= @pageclass %>" class="content">
      <%= yield %>
    </section>

    <%= javascript_include_tag "application" %>
  </body>
</html>
