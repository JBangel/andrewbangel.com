<!DOCTYPE html>
<html class="no-js" lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "Andrew BANGEL" %></title>
    <link rel="stylesheet" media="screen" href="http://openfontlibrary.org/face/generic" rel="stylesheet" type="text/css"/>
    <link rel="stylesheet" media="screen" href="http://openfontlibrary.org/face/paatch" rel="stylesheet" type="text/css"/>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <section id="header">
      <div class="inner">
      <h1><span class="heavy">ANDREW</span>BANGEL</h1>
      <h2>CHARACTER ANIMATOR</h2>
      <div class="navbar">
        <ul>
          <li<%= ' class=selected' if @pageclass == "bio"      %>><%= link_to "BIO", bio_viewer_path %></li>
          <li<%= ' class=selected' if @pageclass == "demoreel" %>><%= link_to "REEL", root_path %></li>
          <li<%= ' class=selected' if @pageclass == "works"    %>><%= link_to "ANIMATIONS", works_path %></li>
          <li><%= link_to "RESUMÉ", "/resume.pdf" %></li>
          <li<%= ' class=selected' if @pageclass == "contact"  %>><%= link_to "CONTACT", contact_viewer_path %></li>
        </ul>
      </div>
      </div>
    </section>

    <% if notice %>
    <div class="flash-notice">
      <span><%= notice %></span>
    </div>
    <br>
    <% end %>

    <section id="<%= @pageclass %>" class="content">
      <%= yield %>
    </section>

    <script src="bower_components/jquery/dist/jquery.min.js"></script>
    <script src="bower_components/foundation/js/foundation.min.js"></script>
    <script src="js/app.js"></script>

    <%= javascript_include_tag "application" %>
  </body>
</html>
